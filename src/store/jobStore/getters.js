export function getSearchResults(state) {
  return state.searchResults;
}

export function getJobList(state) {
  return state.jobList;
}

export function getJobDetails(state) {
  return state.jobDetailsList;
}

export function getSelectedJob(state) {
  return state.selectedJob;
}
